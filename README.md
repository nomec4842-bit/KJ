KJ: web groovebox
=================

## Structure

- `web/` contains the browser UI and pure JavaScript DSP implementation.

## Running the UI

Serve the repository root so the `web/` assets are available to the browser. For example:

```
npx serve .
```

Then open `http://localhost:3000/web/` in your browser.
